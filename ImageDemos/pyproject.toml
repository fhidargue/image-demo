[project]
name = "imagedemos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["image"]

[tool.uv.sources]
image = {path="../Image", editable=true}
